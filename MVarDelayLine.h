#ifndef DELAYLINE_H
#define DELAYLINE_H

// Sets the delay of the delay line.
double setdelay(int M);

// Implements a delay line with the given input sample.
double delayline(double x);

#endif // DELAYLINE_H
#pragma once
